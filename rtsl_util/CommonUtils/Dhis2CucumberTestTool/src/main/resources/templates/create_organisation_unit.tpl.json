{
  "id": "${organisationUnitId}",
  "name": "${organisationUnitName}",
  "shortName": "${organisationUnitShortName}",
  "openingDate": "${organisationUnitOpeningDate}",
  "level": "${organisationUnitLevel}"
  <#if organisationUnitLevel != 1>
  ,"parent": {
      "id": "${parentOrganisationUnitId}"
    }
  </#if>
}
