{
  "id": "${orgUnit.id}",
  "name": "${orgUnit.name}",
  "shortName": "${orgUnit.shortName}",
  "openingDate": "${orgUnit.openingDate}",
  "level": "${orgUnit.level}"
  <#if orgUnit.level != "1">
  , "parent": {
    "id": "${orgUnit.parentId}"
  }
  </#if>
}
