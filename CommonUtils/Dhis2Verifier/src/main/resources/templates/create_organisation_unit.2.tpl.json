{
  "organisationUnits": [
    <#list orgUnits as orgUnit>
    {

      "id": "${orgUnit.id}",
      "name": "${orgUnit.name}",
      "shortName": "${orgUnit.shortName}",
      "openingDate": "${orgUnit.openingDate}",
      "level": "${orgUnit.level}",
      "parent": {
        "id": "${orgUnit.parentId}"
    }}
    <#sep>, </#sep>
    </#list>
  ]
}

