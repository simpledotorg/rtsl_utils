{
  "trackedEntities": [
    {
      "orgUnit": "${data.orgUnitId}",
      "trackedEntity": "${data.teiId}",
      "trackedEntityType": "MCPQUTHX1Ze",
      "attributes": []
    }
  ],
  "enrollments": [
    {
      "enrollment": "${data.enrollmentId}",
      "enrolledAt": "${data.enrolledAt}",
      "occurredAt": "${data.enrolledAt}",
      "orgUnit": "${data.orgUnitId}",
      "trackedEntity": "${data.teiId}",
      "trackedEntityType": "MCPQUTHX1Ze",
      "program": "pMIglSEqPGS",
      "status": "COMPLETED",
      "attributes": [
        <#list dataTable as currentKey, currentValue>
        {
          "attribute": "${currentKey}",
          "value": "${currentValue}"
        }<#sep>, </#sep>
        </#list>
      ]
    }
  ]
}
